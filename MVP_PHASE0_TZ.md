# ТЗ: Быстрый MVP — Конспектирование созвонов

> **Проект:** Отдельный репозиторий, независимый от основного customer-intelligence  
> **Цель:** Запустить рабочую систему за 1–2 недели на готовых инструментах  
> **Стратегия:** "Buy over Build" — никакого самописного бэкенда  
> **Дата:** 2026-02-18

---

## Что это такое (в одном абзаце)

Менеджер проводит созвон в Jitsi. Встреча автоматически записывается и попадает на сервер. n8n замечает новый файл, отправляет его в Яндекс SpeechKit, получает транскрипт и кладёт его в open-notebook к нужному клиенту. Раз в сутки n8n собирает все записи дня, суммаризирует через GLM-4 и отправляет дайджест в Telegram-чат кураторов.

---

## Схема (5 шагов)

```
[Jitsi Meet]
     │  Комната с именем: LEAD-{ID}-conf
     ▼
[Jibri] — записывает митинг
     │  Файл: {ID}_YYYY-MM-DD_HH-MM.webm
     ▼
[NFS папка: /recordings/YYYY/MM/DD/]
     │
     ▼  (n8n следит за папкой)
[n8n Workflow 1: New Recording]
     │  1. Парсит LEAD_ID из имени файла
     │  2. Отправляет в Yandex SpeechKit → получает транскрипт
     │  3. Создаёт заметку в open-notebook для этого клиента
     ▼
[open-notebook] — хранит транскрипты по клиентам
     │
     ▼  (каждую ночь в 23:00)
[n8n Workflow 2: Daily Report]
     │  1. Забирает все заметки за сегодня
     │  2. Суммаризирует каждый митинг через GLM-4
     │  3. Формирует markdown-дайджест
     ▼
[Telegram чат кураторов]
```

---

## Компоненты и ответственность

| Компонент | Кто настраивает | Сложность |
|-----------|----------------|-----------|
| Jitsi Meet + Jibri | **Руководитель** | Высокая (много подводных камней) |
| NFS сервер | **Руководитель** | Средняя |
| VPS для n8n | **Продакт** | Низкая |
| n8n (self-hosted) | **Продакт** | Низкая |
| open-notebook | **Продакт** | Низкая |
| Workflow 1 (транскрипция) | **Продакт** | Средняя |
| Workflow 2 (дайджест) | **Продакт** | Средняя |
| Telegram bot | **Продакт** | Низкая |

---

## Шаг 1: Jitsi + Jibri (делает руководитель)

### Задача
Все митинги с префиксом `LEAD-` в названии комнаты автоматически записываются и сохраняются на NFS.

### Нейминг комнат
- **Формат:** `LEAD-{ID}-conf`
- **Пример:** `LEAD-12345-conf` → ID клиента = `12345`
- Менеджер при создании созвона обязан использовать этот формат

### Jibri finalize script
```bash
#!/bin/bash
# /opt/jibri/finalize.sh
# Вызывается Jibri после окончания записи

RECORDING_FILE=$1
ROOM_NAME=$2
DATE=$(date +%Y/%m/%d)

# Извлечь LEAD_ID из имени комнаты (LEAD-12345-conf → 12345)
LEAD_ID=$(echo $ROOM_NAME | grep -oP '^LEAD-\K\d+')

if [ -z "$LEAD_ID" ]; then
  echo "Не LEAD комната, пропускаем: $ROOM_NAME"
  exit 0
fi

# Создать папку по дате
mkdir -p /recordings/$DATE

# Переименовать и переложить файл
NEW_NAME="${LEAD_ID}_$(date +%Y-%m-%d_%H-%M).webm"
mv "$RECORDING_FILE" "/recordings/$DATE/${NEW_NAME}"

echo "Сохранено: /recordings/$DATE/${NEW_NAME}"
```

### Структура папки на NFS
```
/recordings/
└── 2026/
    └── 02/
        └── 18/
            ├── 12345_2026-02-18_14-30.webm
            ├── 67890_2026-02-18_15-00.webm
            └── ...
```

### Временный workaround для тестирования
Пока Jibri не настроен — вручную складывать `.webm` / `.mp3` файлы в папку `/recordings/YYYY/MM/DD/` с именем `{LEAD_ID}_YYYY-MM-DD_HH-MM.ext`. n8n подхватит их автоматически.

---

## Шаг 2: VPS для n8n (делает продакт)

### Требования к серверу
- CPU: 2 vCPU
- RAM: 4–8 GB
- OS: Ubuntu 22.04 LTS
- Диск: 20 GB
- Стоимость: ~1,500–2,000 руб/мес

### Монтирование NFS
```bash
# Установить клиент
apt install nfs-common

# Смонтировать папку с записями
mount -t nfs {NFS_SERVER_IP}:/recordings /mnt/recordings

# Автомонтирование при перезагрузке
echo "{NFS_SERVER_IP}:/recordings /mnt/recordings nfs defaults 0 0" >> /etc/fstab
```

---

## Шаг 3: Поднять n8n + open-notebook (делает продакт)

### docker-compose.yml
```yaml
version: '3.8'

services:
  n8n:
    image: n8nio/n8n:latest
    restart: unless-stopped
    ports:
      - "5678:5678"
    volumes:
      - /mnt/recordings:/recordings:ro   # NFS папка (read-only)
      - n8n_data:/home/node/.n8n
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=admin
      - N8N_BASIC_AUTH_PASSWORD=СМЕНИТЬ
      - WEBHOOK_URL=http://{VPS_IP}:5678

  open-notebook:
    image: lfnovo/open_notebook:latest
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - notebook_data:/app/data
    environment:
      - OPENAI_API_KEY=not-needed       # можно заглушку если не используем OpenAI
      - ANTHROPIC_API_KEY=not-needed

volumes:
  n8n_data:
  notebook_data:
```

> **open-notebook:** Имеется в виду https://github.com/lfnovo/open-notebook — self-hosted аналог Google NotebookLM. Если это не то — уточнить у руководства.

### Запуск
```bash
docker compose up -d
```

- n8n: `http://{VPS_IP}:5678`
- open-notebook: `http://{VPS_IP}:3000`

---

## Шаг 4: Workflow 1 — Новый файл → Транскрипт → open-notebook

### Что делает
1. Следит за папкой `/recordings`
2. При появлении нового файла — парсит `LEAD_ID` из имени
3. Отправляет аудио в Yandex SpeechKit
4. Кладёт транскрипт в open-notebook к нужному клиенту

### n8n Workflow (создать вручную или импортировать JSON)

```json
{
  "name": "New Recording → Transcription",
  "nodes": [
    {
      "name": "Watch Recordings Folder",
      "type": "n8n-nodes-base.localFileTrigger",
      "parameters": {
        "triggerOn": "fileCreated",
        "path": "/recordings"
      }
    },
    {
      "name": "Parse LEAD_ID from filename",
      "type": "n8n-nodes-base.code",
      "parameters": {
        "jsCode": "const filename = $input.item.json.name;\nconst match = filename.match(/^(\\d+)_/);\nif (!match) throw new Error('Не LEAD файл: ' + filename);\nreturn { json: { leadId: match[1], filename, path: $input.item.json.path } };"
      }
    },
    {
      "name": "Yandex SpeechKit",
      "type": "n8n-nodes-base.httpRequest",
      "parameters": {
        "method": "POST",
        "url": "https://transcribe.api.cloud.yandex.net/speech/stt/v2/longRunningRecognize",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "headers": {
          "Authorization": "Api-Key {{ $env.YANDEX_API_KEY }}"
        },
        "body": {
          "config": {
            "specification": {
              "languageCode": "ru-RU",
              "model": "general",
              "audioEncoding": "OGG_OPUS"
            }
          },
          "audio": {
            "uri": "https://storage.yandexcloud.net/{{ $env.YC_BUCKET }}/{{ $json.filename }}"
          }
        }
      }
    },
    {
      "name": "Save to open-notebook",
      "type": "n8n-nodes-base.httpRequest",
      "parameters": {
        "method": "POST",
        "url": "http://open-notebook:3000/api/notebooks",
        "body": {
          "title": "Созвон {{ $json.date }}",
          "content": "{{ $json.transcript }}",
          "notebookId": "LEAD-{{ $json.leadId }}"
        }
      }
    }
  ]
}
```

> **Примечание по Yandex SpeechKit:** Для длинных файлов (>1 мин) нужно использовать асинхронный API `longRunningRecognize` + polling. Для коротких файлов (<1 мин) — синхронный `stt:recognize`. Нужны: API-ключ Yandex Cloud + S3 bucket для хранения файлов перед отправкой.

### Yandex SpeechKit — что нужно заранее
- [ ] Аккаунт в Yandex Cloud
- [ ] Создать API-ключ (IAM или API Key)
- [ ] Создать S3 bucket в Object Storage
- [ ] Уточнить у руководства: есть ли уже аккаунт и бюджет?

---

## Шаг 5: Workflow 2 — Ежедневный дайджест в Telegram

### Что делает
Каждый день в 23:00:
1. Забирает все заметки за сегодня из open-notebook
2. Суммаризирует каждый митинг через GLM-4
3. Формирует markdown-отчёт
4. Отправляет в Telegram-чат кураторов

### n8n Workflow

```json
{
  "name": "Daily Summary Report",
  "nodes": [
    {
      "name": "Cron 23:00",
      "type": "n8n-nodes-base.cron",
      "parameters": {
        "cronExpression": "0 23 * * *"
      }
    },
    {
      "name": "Get Today Notes from open-notebook",
      "type": "n8n-nodes-base.httpRequest",
      "parameters": {
        "method": "GET",
        "url": "http://open-notebook:3000/api/notes",
        "queryParameters": {
          "date": "={{ $now.format('YYYY-MM-DD') }}"
        }
      }
    },
    {
      "name": "Summarize with GLM-4",
      "type": "n8n-nodes-base.httpRequest",
      "parameters": {
        "method": "POST",
        "url": "https://open.bigmodel.cn/api/paas/v4/chat/completions",
        "headers": {
          "Authorization": "Bearer {{ $env.GLM4_API_KEY }}"
        },
        "body": {
          "model": "glm-4-flash",
          "messages": [
            {
              "role": "system",
              "content": "Ты бизнес-аналитик. Сделай краткое саммари созвона (3-5 предложений). Выдели ключевые договорённости и action items."
            },
            {
              "role": "user",
              "content": "={{ $json.content }}"
            }
          ]
        }
      }
    },
    {
      "name": "Build Report",
      "type": "n8n-nodes-base.code",
      "parameters": {
        "jsCode": "const items = $input.all();\nlet report = `📊 *Дайджест созвонов — ${$now.format('DD.MM.YYYY')}*\\n\\n`;\nreport += `Всего митингов: ${items.length}\\n\\n`;\nfor (const item of items) {\n  report += `📦 *${item.json.clientName || 'Клиент LEAD-' + item.json.leadId}*\\n`;\n  report += `${item.json.summary}\\n\\n`;\n}\nreturn { json: { report } };"
      }
    },
    {
      "name": "Send to Telegram",
      "type": "n8n-nodes-base.httpRequest",
      "parameters": {
        "method": "POST",
        "url": "https://api.telegram.org/bot{{ $env.TELEGRAM_BOT_TOKEN }}/sendMessage",
        "body": {
          "chat_id": "{{ $env.TELEGRAM_CHAT_ID }}",
          "text": "={{ $json.report }}",
          "parse_mode": "Markdown"
        }
      }
    }
  ]
}
```

### Telegram bot — что нужно заранее
- [ ] Создать бота через @BotFather → получить токен
- [ ] Добавить бота в нужный чат
- [ ] Получить `chat_id` (например через `https://api.telegram.org/bot{TOKEN}/getUpdates`)

---

## Переменные окружения (n8n)

Задаются в n8n → Settings → Environment Variables:

| Переменная | Где взять |
|------------|-----------|
| `YANDEX_API_KEY` | Yandex Cloud Console → API Keys |
| `YC_BUCKET` | Имя S3 bucket в Yandex Object Storage |
| `GLM4_API_KEY` | https://open.bigmodel.cn → API Keys |
| `TELEGRAM_BOT_TOKEN` | @BotFather |
| `TELEGRAM_CHAT_ID` | getUpdates API или @userinfobot |

---

## Стоимость эксплуатации

| Компонент | Стоимость/мес |
|-----------|---------------|
| VPS 2 vCPU / 4GB RAM | ~1,500 руб |
| Yandex SpeechKit | ~25,000 руб (220ч аудио) |
| GLM-4-Flash API | ~300–500 руб |
| **ИТОГО** | **~27,000 руб/мес** |

> После проверки концепции — рассмотреть переход на self-hosted Whisper (~0 руб на STT).

---

## Чеклист запуска (для продакта)

### Подготовка (до старта)
- [ ] Уточнить у руководства: что такое "Open Notebook"? (ссылка или название)
- [ ] Уточнить: есть ли Yandex Cloud аккаунт и API-ключ?
- [ ] Уточнить: в какой Telegram-чат слать дайджест и кто получатели?
- [ ] Уточнить: формат ID Лида (число, строка, из какой системы?)
- [ ] Уточнить: NFS сервер — IP и путь к папке?

### Инфраструктура
- [ ] Арендовать VPS (Ubuntu 22.04, 2 vCPU, 4–8 GB RAM)
- [ ] Смонтировать NFS: `/mnt/recordings`
- [ ] Поднять docker-compose (n8n + open-notebook)
- [ ] Проверить: `http://{VPS_IP}:5678` открывается, `http://{VPS_IP}:3000` открывается

### Yandex SpeechKit
- [ ] Создать/получить API-ключ
- [ ] Создать S3 bucket для аудиофайлов
- [ ] Протестировать вручную: загрузить файл, получить транскрипт

### GLM-4
- [ ] Получить API-ключ на https://open.bigmodel.cn
- [ ] Протестировать вручную: отправить промпт, получить ответ

### Telegram
- [ ] Создать бота через @BotFather
- [ ] Добавить бота в чат кураторов
- [ ] Получить chat_id

### n8n Workflows
- [ ] Создать Workflow 1 (New Recording → Transcription)
- [ ] Создать Workflow 2 (Daily Report)
- [ ] Задать переменные окружения
- [ ] Тест Workflow 1: вручную положить файл в `/recordings`, убедиться что создалась заметка в open-notebook
- [ ] Тест Workflow 2: запустить вручную, убедиться что дайджест пришёл в Telegram

### Финальная проверка
- [ ] Провести тестовый митинг в Jitsi с именем комнаты `LEAD-{ID}-conf`
- [ ] Убедиться что файл появился в NFS
- [ ] Убедиться что транскрипт появился в open-notebook
- [ ] Дождаться 23:00 или запустить Workflow 2 вручную → получить дайджест в Telegram

---

## Открытые вопросы (получить ответы у руководства до старта)

1. **Open Notebook:** Это https://github.com/lfnovo/open-notebook или что-то другое?
2. **Yandex Cloud:** Есть ли уже аккаунт? Какой месячный бюджет на STT?
3. **Telegram:** В какой чат слать? Только кураторы или все менеджеры?
4. **ID Лида:** Откуда берётся ID? Из Bitrix24? Какой формат — только цифры?
5. **NFS:** IP сервера и путь к папке после того как руководитель настроит Jibri
6. **Workaround:** Куда вручную складывать тестовые файлы пока Jibri не готов?

---

## Связь с основным проектом (customer-intelligence)

Этот MVP — **временный шорткат** для быстрой демонстрации ценности.  
Основной проект (FastAPI + PostgreSQL + pgvector) — **целевая архитектура**.

После успешного MVP данные из open-notebook можно мигрировать в основную систему через n8n. Либо open-notebook просто останется UI-слоем поверх основного API.

---

*Документ создан: 2026-02-18 | Версия: 1.0*
