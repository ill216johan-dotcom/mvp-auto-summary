# Техническая реализация синхронизации Bitrix

> **Дата:** 2026-03-11
> **Статус:** В разработке

---

## Архитектура

```
┌─────────────────────────────────────────────────────────────────┐
│                    Bitrix24 CRM                                │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐ │
│  │ Лиды        │  │ Контакты    │  │ Активности              │ │
│  │ (статус 1-7)│  │ (все)       │  │ (звонки, email, комms)   │ │
│  └─────────────┘  └─────────────┘  └─────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                 Python Orchestrator                             │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ sync_bitrix_leads()   → фильтр статусов 1-7               ││
│  │ sync_bitrix_contacts() → извлечение договоров               ││
│  │ sync_calls()          → активности TYPE_ID=2               ││
│  │ sync_emails()         → активности TYPE_ID=4               ││
│  │ sync_comments()       → timeline комментарии                ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                    PostgreSQL                                  │
│  bitrix_leads    → лиды/контакты с diffy_lead_id               │
│  bitrix_calls    → звонки с транскрипциями                     │
│  bitrix_emails   → письма                                       │
│  bitrix_comments → комментарии сотрудников                      │
│  bitrix_summaries → ежедневные саммари                         │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                     LLM (Claude/GML)                            │
│  generate_bitrix_summaries() → AI саммари за каждый день       │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                       Dify RAG                                  │
│  Датасеты: "ФФ-4405 / ФФ-2577"                                 │
│  Документы: саммари за каждый день взаимодействия              │
└─────────────────────────────────────────────────────────────────┘
```

---

## Обработка договоров

### Извлечение номеров договоров

**Источник данных (в порядке приоритета):**
1. UF поле `UF_CRM_1632960743049` ("Номера договоров" в UI)
2. Поле `SECOND_NAME` контакта (часто содержит договоры)
3. Поле `TITLE` (fallback)

**Формат:** "ФФ-2577 / ФФ-4405" (через `/`)

### Логика определения активного договора

```python
# В Bitrix: ПОСЛЕДНИЙ в строке = активный
"ФФ-2577 / ФФ-4405" → активный = "ФФ-4405"
```

### Использование в системе

| Поле | Значение | Пример |
|------|----------|--------|
| `diffy_lead_id` | Активный договор | `ФФ-4405` |
| `contract_number` | Все договоры, активный первый | `ФФ-4405 / ФФ-2577` |
| Название датасета Dify | Все договоры | `ФФ-4405 / ФФ-2577` |
| Заголовок саммари | Все договоры | `# Договоры: ФФ-4405 / ФФ-2577` |

**Поиск в RAG:** работает по любому договору (и ФФ-4405, и ФФ-2577)

---

## Фильтрация лидов

**Синхронизируются ТОЛЬКО лиды со статусом 1-7:**

| STATUS_ID | Название | Синхронизируется? |
|-----------|----------|-------------------|
| 1 | В работе (ответственность принята) | ✅ |
| 2 | Первичный недозвон | ✅ |
| 3 | Вышел на связь | ✅ |
| 4 | В процессе общения | ✅ |
| 5 | Meet назначен | ✅ |
| 6 | Экскурсия назначена | ✅ |
| 7 | Договор | ✅ |
| NEW | Новый | ❌ |
| JUNK | Мусор | ❌ |
| 13 | Закрыт/неактивен | ❌ |
| 23 | Преобразован в контакт | ❌ (для лидов) |

**Контакты:** синхронизируются ВСЕ (без фильтра статусов)

---

## Обработка звонков

### ⚠️ ВАЖНО: Проблема phone_number (ИСПРАВЛЕНО 2026-03-13)

**Симптомы:**
- Звонки синхронизировались, но `phone_number` был NULL
- Невозможно определить какой номер звонил
- Пропущены важные бизнес-данные

**Корневая причина:**

1. **`SETTINGS.CALL_ID` пустой** для старых звонков (до июня 2025)
   - API возвращает: `"SETTINGS": []`
   - VoxImplant enrichment не работал (не было связи activity ↔ voximplant)

2. **Телефон был в `COMMUNICATIONS[0].VALUE`**
   - Код не извлекал его из `crm.activity.list`
   - Ожидал enrichment из voximplant (который не работал)

**Исследование (ФФ-4405, 66 звонков):**

```bash
# API возвращает:
curl -X POST "https://bitrix24.ff-platform.ru/rest/1/.../crm.activity.list" \
  -d '{"filter":{"OWNER_TYPE_ID":3,"OWNER_ID":5723,"TYPE_ID":2}}'

# Ответ:
{
  "ID": "150513",
  "TYPE_ID": "2",
  "SETTINGS": [],  # ПУСТОЙ!
  "COMMUNICATIONS": [
    {
      "TYPE": "PHONE",
      "VALUE": "+79135379385"  # ТЕЛЕФОН ЗДЕСЬ!
    }
  ]
}
```

**Решение (реализовано в коммитах 2a90924, f4858dc):**

```python
# Извлечь телефон из COMMUNICATIONS
communications = activity.get("COMMUNICATIONS") or []
phone_number = ""
if communications and len(communications) > 0:
    comm = communications[0]
    if comm.get("TYPE") == "PHONE":
        phone_number = comm.get("VALUE") or ""
```

**Результат для ФФ-4405:**
```
До исправления:
phone_number | COUNT
-------------+-------
NULL         |    66

После исправления:
phone_number  | COUNT
--------------+-------
+79099358635  |    25
+79135379385  |    41
```

**Технические детали:**

1. **Primary Source:** `activity.COMMUNICATIONS[0].VALUE`
   - Надёжный источник для ВСЕХ звонков
   - Доступен в `crm.activity.list`

2. **Secondary Source:** `voximplant.statistic.get.PHONE_NUMBER`
   - Fallback для enrichment
   - Работает только для новых звонков (после июня 2025)

3. **ON CONFLICT LIMITATION:**
   - `ON CONFLICT DO UPDATE` не обновляет NULL → value
   - Требуется отдельный шаг enrichment для существующих записей

### Типы звонков и транскрибация (РЕШЕНО 2026-03-13)

**Проблема:** Изначально система пыталась использовать прямые URL `vats528994.megapbx.ru`, но они оказались заблокированы/недоступны (404/timeout).

**Решение:**
1. Использовать `voximplant.statistic.get` с `SELECT=["RECORD_FILE_ID"]`.
2. Скачивать файлы через внутреннее API диска: `disk.file.get?id=RECORD_FILE_ID` -> получение временного `DOWNLOAD_URL`.
3. Скачивать реальный MP3 и отправлять напрямую в Whisper (`/v1/audio/transcriptions`).

| Период | Обработка | Транскрибация | phone_number |
|--------|-----------|---------------|---------------|
| До июня 2025 | Кол-во ✅ | ❌ Записи удалены Битриксом | ✅ COMMUNICATIONS |
| После июня 2025 | Транскрипция ✅ | ✅ Whisper (через Disk API) | ✅ COMMUNICATIONS + voximplant |

### API Bitrix для звонков

- **Owner Type:** `3` = CONTACT, `1` = LEAD
- **Activity Type:** `TYPE_ID=2` = Meetings (содержит звонки)
- **Phone Number:** `COMMUNICATIONS[0].VALUE` (TYPE=PHONE)
- **Запись:** `voximplant.statistic.get` → `RECORD_FILE_ID`
- **Скачивание:** `disk.file.get` → `DOWNLOAD_URL`

---

## Структура данных

### Таблица `bitrix_leads`

```sql
bitrix_lead_id      -- ID в Bitrix
bitrix_entity_type  -- 'lead' или 'contact'
diffy_lead_id       -- Уникальный ID для поиска (активный договор)
title               -- Название из Bitrix
name                -- Имя контакта
phone               -- Телефон
email               -- Email
status_id           -- Статус (только для лидов)
contract_number     -- Все договоры: "ФФ-4405 / ФФ-2577"
```

### Таблица `bitrix_summaries`

```sql
diffy_lead_id       -- ФФ-4405
summary_date        -- 2026-02-25
calls_count         -- 3
summary_text        -- Текст саммари с заголовками
dify_doc_id         -- ID документа в Dify
```

---

## Ключевые функции

### `_extract_contract_number(value)`
Возвращает активный (последний) договор из строки.
```python
"ФФ-2577 / ФФ-4405" → "ФФ-4405"
```

### `_extract_all_contracts(value)`
Возвращает список всех договоров.
```python
"ФФ-2577 / ФФ-4405" → ["ФФ-2577", "ФФ-4405"]
```

### `_format_contract_display(value)`
Возвращает все договоры, активный первым.
```python
"ФФ-2577 / ФФ-4405" → "ФФ-4405 / ФФ-2577"
```

---

## RAG Chat в Dify

### Настройка

1. **Model Providers:**
   - LLM: Anthropic → glm-5
   - Embeddings: OpenAI-compatible → `http://84.252.100.93:8081/v1`

2. **Chatbot → Model:** glm-5 (только LLM для ответов)

3. **Chatbot → Context → Knowledge:**
   - Выбрать датасеты для поиска

### Поиск по договорам

```
Вопрос: "Что известно о ФФ-4405?"
→ Ищет в датасете "ФФ-4405 / ФФ-2577"

Вопрос: "Что было у ФФ-2577?"
→ Тоже находит (документ содержит оба номера)
```

---

## Скрипты

### Ежедневная синхронизация
```bash
# Запускается по расписанию (22:00 по Мск)
docker exec mvp-auto-summary-orchestrator-1 python -m app.main
```

### Историческая синхронизация
```bash
# Полная пересинхронизация всех данных
PYTHONPATH=/root/mvp-auto-summary python3 scripts/run_historical_sync.py
```

---

## Тестирование

### Контакт 5723 (Алексей)

| Поле | Значение |
|------|----------|
| Bitrix ID | 5723 |
| `diffy_lead_id` | ФФ-4405 |
| `contract_number` | ФФ-4405 / ФФ-2577 |
| Звонков | 66 |
| Период | 2022-11-23 → 2026-02-25 |
| Дней с активностями | 34 |

---

**Последнее обновление:** 2026-03-11
**Версия:** 1.0
