# Инструкции по деплою — 2026-02-21

## Что было исправлено

### Workflow 01 (New Recording)
- **Save Success?** — исправлена логика проверки успеха сохранения в open-notebook
  - Было: `$json.error == ""` (не работает при `undefined`)
  - Стало: `$json.id || $json.source_id` не пустой

### Workflow 02 (Daily Digest)
- **GLM-4 Summarize** — захардкожен API ключ и endpoint
  - Endpoint: `https://open.bigmodel.cn/api/paas/v4/chat/completions`
  - Model: `glm-4.7-flash`
  - Добавлено: `thinking: { type: "disabled" }` — отключает thinking mode
- **Send Telegram** — захардкожены токен и chat_id
  - Token: из документации
  - Chat ID: из документации
- **Build Digest** — добавлен fallback на `reasoning_content`
  - Если `content` пустой, берётся `reasoning_content`

---

## Инструкция для PuTTY

### Шаг 1: Подключиться к серверу
```bash
ssh root@84.252.100.93
# Пароль: xe1ZlW0Rpiyk
```

### Шаг 2: Проверить статус контейнеров
```bash
docker ps --format "table {{.Names}}\t{{.Status}}"
```

Все контейнеры должны быть "Up":
- mvp-auto-summary-n8n-1
- mvp-auto-summary-postgres-1
- mvp-auto-summary-surrealdb-1
- mvp-auto-summary-open-notebook-1
- mvp-auto-summary-whisper-1

### Шаг 3: Загрузить обновлённые workflows

**Вариант A: Через n8n UI (рекомендуется)**

1. Открой http://84.252.100.93:5678
2. Логин: `rod@zevich.ru` / `Ill216johan511lol2`
3. Для каждого workflow:
   - Открой workflow
   - Нажми на три точки → Import from File
   - Или обнови ноды вручную:

**Workflow 01 — изменения:**
- Нода `Save Success?`:
  - Измени условие: `{{ $json.id || $json.source_id || $json.command_id }}` not equal to empty

**Workflow 02 — изменения:**
- Нода `GLM-4 Summarize`:
  - URL: `https://open.bigmodel.cn/api/paas/v4/chat/completions`
  - Authorization: `Bearer fda5cc088ab04a1a92d5966b373e81a3.rfUescuUieAO78M6`
  - В jsonBody добавить: `"thinking": { "type": "disabled" }`
  - model: `glm-4.7-flash`

- Нода `Send Telegram`:
  - URL: `https://api.telegram.org/bot8527521201:AAHpyrPn4cig-zq0Xymt7lZ94qBIEXnYAeQ/sendMessage`
  - chat_id: `-1003872092456`

- Нода `Build Digest`:
  - Добавить fallback:
    ```javascript
    const msg = ($json.choices && $json.choices[0] && $json.choices[0].message) ? $json.choices[0].message : {};
    const rawContent = (msg.content || '').trim();
    const rawReasoning = (msg.reasoning_content || '').trim();
    const summaryText = rawContent || rawReasoning || 'Сводка не получена.';
    ```

**Вариант B: Через API (если есть API Key)**

```bash
# Получить API Key из n8n UI: Settings -> n8n API -> Create API Key
export N8N_API_KEY="твой-api-key"

# Загрузить workflows
cd /root/mvp-auto-summary/scripts
chmod +x deploy-workflows.sh
./deploy-workflows.sh
```

### Шаг 4: Протестировать

**Тест Workflow 01:**
```bash
# Создать тестовый файл
mkdir -p /mnt/recordings/2026/02/21/
dd if=/dev/urandom bs=96000 count=1 > /mnt/recordings/2026/02/21/99999_2026-02-21_12-00.wav

# Или использовать реальный файл с речью!
```

Затем в n8n UI: Workflow 01 → Execute Workflow

**Проверить результат:**
```bash
docker exec mvp-auto-summary-postgres-1 psql -U n8n -d n8n \
  -c "SELECT id, filename, status, LENGTH(transcript_text) as txt_len FROM processed_files ORDER BY id DESC LIMIT 5;"
```

**Тест Workflow 02:**
```bash
# Сбросить summary_sent для тестовых данных
docker exec mvp-auto-summary-postgres-1 psql -U n8n -d n8n \
  -c "UPDATE processed_files SET summary_sent = false WHERE status = 'completed';"
```

Затем в n8n UI: Workflow 02 → Execute Workflow

Проверить Telegram — должно прийти сообщение.

### Шаг 5: Проверить логи при ошибках

```bash
# Логи n8n
docker logs mvp-auto-summary-n8n-1 --tail=100

# Логи open-notebook
docker logs mvp-auto-summary-open-notebook-1 --tail=50

# Логи whisper
docker logs mvp-auto-summary-whisper-1 --tail=50
```

---

## Если что-то не работает

### Проблема: n8n не видит файлы
```bash
docker exec mvp-auto-summary-n8n-1 find /recordings -type f -name "*.wav" | head -5
```

### Проблема: Whisper не отвечает
```bash
curl -s http://localhost:9000/health
```

### Проблема: open-notebook не работает
```bash
curl -s http://localhost:8888/notebooks
```

### Проблема: GLM-4 возвращает ошибку
```bash
curl -s -X POST https://open.bigmodel.cn/api/paas/v4/chat/completions \
  -H "Authorization: Bearer fda5cc088ab04a1a92d5966b373e81a3.rfUescuUieAO78M6" \
  -H "Content-Type: application/json" \
  -d '{"model":"glm-4.7-flash","messages":[{"role":"user","content":"test"}],"max_tokens":10,"thinking":{"type":"disabled"}}'
```

---

## Полезные команды

```bash
# Очистить все обработанные файлы (для тестирования)
docker exec mvp-auto-summary-postgres-1 psql -U n8n -d n8n -c "DELETE FROM processed_files;"

# Перезапустить все контейнеры
cd /root/mvp-auto-summary && docker compose restart

# Перезапустить только n8n
docker compose restart n8n
```
